# Normaliza finales de línea en todo el texto
* text=auto eol=lf

# Fuentes de verdad y ficheros generados
*.json text
*.md   text diff=markdown
*.txt  text

# Nada de Git LFS: en este repo no entran binarios. El audio y el
# vídeo van a Google Drive, no a git.
